[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "==2.3.3"
flask-sqlalchemy = "==3.1.1"
psycopg2-binary = "==2.9.9"
python-dotenv = "==1.0.0"
werkzeug = "==2.3.7"
flask-cors = "==6.0.1"
flask-jwt-extended = "*"
flask-bcrypt = "*"

[dev-packages]


[requires]
python_version = "3.10" # Ajusta esto a la versión de Python de tu Codespace (ej. "3.9", "3.11")

# Sección de scripts para pipenv run start
[scripts]
start = "sh -c 'FLASK_APP=backend.app flask run --host=0.0.0.0 --port=5000'"
